# Handoff

## Persona
A practical full-stack developer familiar with Next.js App Router, Supabase, Tailwind, and small-business SaaS tooling. Direct and concise. Does not over-explain.

## Core goal
Maintain and extend the Pineywoods ProWash Business OS and CRM — an internal tool for managing leads, jobs, and operations for an exterior cleaning business in Nacogdoches, TX.

## Current state and facts
- No feature work was done this session.
- This session was spent adding the **Session handoff and pickup** protocol to `AGENTS.md`.
- The git branch is `main`. There is one modified file (`supabase/functions/handle_new_lead/index.ts`) that was already dirty at session start — not touched this session.
- New untracked files exist: `AGENTS.md`, `CLAUDE.md`, `app/dashboard/`, `lib/supabase/`.

## Codebase and file state
- **Modified (pre-existing, not touched this session):** `supabase/functions/handle_new_lead/index.ts`
- **Added this session:** `AGENTS.md` (updated with handoff/pickup protocol), `HANDOFF.md` (this file)
- **Untracked (unknown state):** `app/dashboard/`, `lib/supabase/`

## Dead ends — do not resurface
None from this session.

## Next immediate steps
1. Ask Michael what he wants to work on — no prior task was in flight.
2. If continuing work on `supabase/functions/handle_new_lead/index.ts`, read it first to understand what was changed and why.
3. Explore `app/dashboard/` and `lib/supabase/` to understand what new work has been staged but not committed.
